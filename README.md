### App uses [Astro](https://docs.astro.build/en)

### Install [bun](https://bun.sh/), works in WSL2 not Windows at the moment

To run the app locally, run `bun install` and then `bun dev` in the root directory.
